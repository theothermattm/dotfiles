# assumes git is installed
source ~/git-completion.bash

# pull in common stuff
source ~/.matt_profile

# will probably need to do something like this
# override os x's default vi and use homebrew's version of macvim
# function vi() { /usr/local/Cellar/macvim/7.4-72/bin/vi "$@" ;}
